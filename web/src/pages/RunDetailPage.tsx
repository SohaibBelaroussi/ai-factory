import { PageHeader, Empty } from '../components/ui';

export default function RunDetailPage(): React.ReactNode {
  return (
    <div>
      <PageHeader title="Run" />
      <Empty>run detail — next milestone</Empty>
    </div>
  );
}
