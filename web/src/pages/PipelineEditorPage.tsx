import { PageHeader, Empty } from '../components/ui';

export default function PipelineEditorPage(): React.ReactNode {
  return (
    <div>
      <PageHeader title="Pipeline" />
      <Empty>pipeline editor — next milestone</Empty>
    </div>
  );
}
